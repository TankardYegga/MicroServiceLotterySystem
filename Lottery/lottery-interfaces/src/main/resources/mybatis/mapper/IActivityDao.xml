<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.infrastructure.dao.IActivityDao">

    <resultMap id="activityMap" type="org.example.infrastructure.po.Activity">
        <id column="id" property="id"/>
        <result column="activityId" property="activityId"/>
        <result column="activityName" property="activityName"/>
        <result column="activityDesc" property="activityDesc"/>
        <result column="beginDateTime" property="beginDateTime"/>
        <result column="endDateTime" property="endDateTime"/>
        <result column="stockDount" property="stockCount"/>
        <result column="takeDount" property="takeCount"/>
        <result column="state" property="state"/>
        <result column="creator" property="creator"/>
        <result column="createTime" property="createTime"/>
        <result column="updateTime" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="org.example.infrastructure.po.Activity">
        INSERT INTO activity
        (activityId, activityName, activityDesc, beginDateTime, endDateTime,
         stockCount, takeCount, state, creator, createTime, updateTime)
        VALUES
            (#{activityId}, #{activityName}, #{activityDesc}, #{beginDateTime}, #{endDateTime},
             #{stockCount}, #{takeCount}, #{state}, #{creator}, now(), now())
    </insert>

    <select id="queryActivityById" parameterType="java.lang.Long" resultMap="activityMap">
        SELECT activityId, activityName, activityDesc, begindateTime, endDatetime,
               stockCount, takeCount, state, creator, createTime, updateTime
        FROM activity
        WHERE activityid = #{activityId}
    </select>


</mapper>


